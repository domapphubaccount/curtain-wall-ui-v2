# Local dev (docker-compose): postgresql://sprintforge:sprintforge@localhost:5432/sprintforge
# Hosted (Neon/Supabase/Render/Railway): use the connection string they give you.
DATABASE_URL="postgresql://sprintforge:sprintforge@localhost:5432/sprintforge"
PORT=4000
# Comma-separated list of allowed frontend origins.
CORS_ORIGIN="http://localhost:5173"
