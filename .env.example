# Point this at the SprintForge API server (see server/) to sync projects to a real database.
# Leave unset to keep running purely off localStorage, same as before.
VITE_API_URL="http://localhost:4000"
